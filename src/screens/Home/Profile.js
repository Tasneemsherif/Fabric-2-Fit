import React from 'react';
import {View, Text, SafeAreaView, ScrollView, ImageBackground, TextInput, TouchableOpacity, Image, RefreshControl} from 'react-native';




const Profile = () => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
}

export default Profile;